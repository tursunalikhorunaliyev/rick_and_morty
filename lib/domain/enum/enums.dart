enum CharacterGender { female, male, genderless, unknown }

enum CharacterStatus { alive, dead, unknown }

enum CharacterLoadingType { none, api, getFavorites, addFavorites, removeFavorites }

enum FilterChipType { status, gender }
